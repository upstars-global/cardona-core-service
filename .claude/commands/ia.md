---
description: QA Impact Analysis (short alias for /impact-analysis)
---

Launch the `impact-analysis` subagent via the Agent tool (`subagent_type: "impact-analysis"`) to produce the Cardona Impact Analysis for the current git change set. If I named a specific range or files, pass them in the prompt. The subagent runs in its own context on Sonnet and returns the finished report — relay its final message to me verbatim, adding nothing before or after. The report is in Ukrainian.
