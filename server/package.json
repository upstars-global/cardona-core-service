{
  "name": "@backstop-panel/server",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "cp -r ./_redirects ./public/_redirects && nitro build",
    "dev": "nitro dev",
    "prepare": "nitro prepare",
    "preview": "node .output/server/index.mjs"
  }
}
