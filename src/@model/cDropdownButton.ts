export type CDropdownItems = {
  name: string
  icon?: string
  modalId?: string
  permission: string
}
export enum Ripple {
  'primary' = 'rgba(255, 255, 255, 0.15)',
  'outline-primary' = 'rgba(186, 191, 199, 0.15)',
}
