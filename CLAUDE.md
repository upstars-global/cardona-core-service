# write-tests
- **write-tests** (`.claude/skills/write-tests/SKILL.md`) - writing unit tests following project conventions.
Invoke the Skill tool with `skill: "write-tests"` before doing anything else ONLY when the request is explicitly about writing, creating, or generating tests or spec files — for example: "write tests for X", "create a spec file", "cover this component with tests", "add unit tests".
Do NOT invoke this skill for any other tasks such as implementing features, fixing bugs, refactoring, or explaining code.